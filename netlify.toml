[build]
  command = "npm run build"
  publish = "build"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

4. **Ctrl + S で保存**

---

## 📍 **.nvmrc を作成（まだ作っていない場合）**

### **確認：**
- **左側のファイル一覧に `.nvmrc` がありますか？**
  - **ある** → 次のステップへ
  - **ない** → 以下の手順で作成

### **作成方法：**

1. **左側の `CARE-WATCH-APP` （一番上の太字）を右クリック**
2. **「新しいファイル」をクリック**
3. **ファイル名：** `.nvmrc`
4. **Enter キーを押す**
5. **開いたファイルに以下を入力：**
```
   18